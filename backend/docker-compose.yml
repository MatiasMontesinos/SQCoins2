version: '3.9'
services:
  db:
    ports:
      - 5432:5432
    volumes:
      - ./volumes:/var/lib/postgresql/data
    image: postgres:17
    environment:
      - POSTGRES_PASSWORD=posgres
      - POSTGRES_USER=posgres
      - POSTGRES_DB=sqcoins_db